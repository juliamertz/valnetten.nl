<header>
  <section>
    <h1>VALNETTEN<span>.</span>NL</h1>
  </section>
  <nav>
    <ul>
      <a class="link-underline" href="/"><li>Home</li></a>
      <a class="link-underline" href="/randbeveiliging"
        ><li>Randbeveiliging</li>
      </a>
      <a class="link-underline" href="/diensten"><li>Diensten</li></a>
      <a class="link-underline" href="/contact"><li>Contact</li></a>
    </ul>
  </nav>
</header>

<style>
  header {
    width: 100%;
    display: flex;
    position: sticky;
    flex-flow: row nowrap;
    justify-content: space-between;
    background: white;
    color: black;
    z-index: 30;
    gap: 2rem;
    top: 0;
    padding: 0 3rem;
  }

  header section {
    padding: 1rem 0;
  }

  header section h1 {
    font-size: var(--font-size-base);
    font-weight: 600;
  }

  header section h1 span {
    color: var(--orange-600);
    font-size: 2rem;
  }

  nav ul {
    display: flex;
    list-style: none;
    flex-flow: row nowrap;
    align-items: center;
    height: 100%;
    position: relative;
  }
  nav ul a {
    height: max-content;
    padding: 1rem;
    display: grid;
    place-items: center;
    cursor: pointer;
    height: 100%;
    font-weight: 500;
    transition: background 200ms var(--ease);
    position: relative;
    top: 10px;
  }

  nav ul a::after {
    content: "";
    width: 100%;
    transform: scaleX(0);
    transform-origin: left;
    height: 2px;
    background: var(--orange-600);
    bottom: 10px;
    position: relative;
    transition: all 300ms var(--ease);
  }

  nav ul a:hover::after {
    transform: scaleX(1);
  }

  ul > a {
    text-decoration: none;
    color: black;
    text-transform: uppercase;
    font-weight: 600;
  }

  @media (max-width: 940px) {
    nav {
      display: none;
    }
  }
</style>
